#include "main.h"

/**
 * _isdigit - checks for digits.
 * @d: The digit to be checked.
 *
 * Return: 1 if digit is, 0 otherwise
 */

int _isdigit(int d)
{
	if (d >= 0 && c <= 9)
		return (1);
	else
		return (0);
}
